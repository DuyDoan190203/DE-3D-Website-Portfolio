import React from "react";
import Tilt from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className='xs:w-[250px] w-full'>
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className='w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card'
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className='bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col'
      >
        <img
          src={icon}
          alt={title}
          className='w-16 h-16 object-contain'
        />

        <h3 className='text-white text-[20px] font-bold text-center'>
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className='mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]'
      >
        Started my data engineering journey during my BIT studies in Finland, where I discovered my passion for 
        turning messy data into meaningful insights. Through hands-on experience at Innate AI and Listeds, I've 
        learned that the best data solutions aren't just technically sound—they solve real business problems. 
        My technical toolkit includes AWS (Glue, Redshift, S3, Kinesis, Lambda), Apache Airflow, Kafka, Spark, 
        Terraform, Docker, Kubernetes, dbt, Great Expectations, and both OLTP/OLAP systems. Currently balancing 
        my Arcada University of Applied Sciences Big Data Analytics studies with hands-on practice—because I believe the best data engineers 
        never stop learning. Excited about integrating ML into real-time systems and exploring how AI can 
        enhance human decision-making in the Finnish tech ecosystem.
      </motion.p>

      <div className='mt-20 flex flex-wrap gap-10'>
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
